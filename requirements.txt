# Install PyTorch FIRST, matching your hardware (see docs/inference.md):
#   GPU (RTX 3060):  pip install torch --index-url https://download.pytorch.org/whl/cu128
#   CPU only:        pip install torch --index-url https://download.pytorch.org/whl/cpu
# Versions below are the ones verified on 2026-09-24 (CPU machine, Python 3.12.10).

numpy>=2.0
opencv-python>=4.10
mediapipe>=1.0.1
pyyaml>=6.0
pillow>=10.0
imageio>=2.34
imageio-ffmpeg>=0.5
psutil>=5.9
huggingface_hub>=1.0

# Diffusion baseline (Phase 1)
diffusers>=0.40.0
transformers>=5.0
accelerate>=1.0
sentencepiece>=0.2
ftfy>=6.0
safetensors>=0.4

# Dev / tests
pytest>=8.0
scikit-image>=0.24   # only for the built-in 'astronaut' test image
